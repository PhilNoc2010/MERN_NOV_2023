import React from 'react';

const Weapon = (props) => {
    return (
        <fieldset>
            <legend>Weapon.jsx</legend>
            <p>
                ⚔️
            </p>
        </fieldset>
    );
};

export default Weapon;